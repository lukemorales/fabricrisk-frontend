import { styled } from '../../styles';

export const Container = styled('main', {
  width: '100vw',
  maxWidth: 480,
  padding: '20px 24px',
  margin: 20,
  background: '$shapes',
  borderRadius: 8,
  boxShadow: '0px 12px 10px -8px #404040',
  transition: '180ms ease-in-out',
});
